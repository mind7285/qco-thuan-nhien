// 🇻🇳 View tổng hợp quyền hạn để check logic API/UI
// 🇺🇸 Aggregated view of user rights for API/UI logic checking
export interface M_Vw_Auth_Usr_Right {
  // 🇻🇳 ID người dùng
  // 🇺🇸 User ID
  c_usr_id: string;

  // 🇻🇳 Tên đăng nhập
  // 🇺🇸 Username
  c_usr_name: string;

  // 🇻🇳 Mã module
  // 🇺🇸 Module code
  c_mod_code: string;

  // 🇻🇳 Mã quyền
  // 🇺🇸 Permission code
  c_perm_code: string;
}

